<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Piñateria la 8</title>
    <!-- imagen de penstañ -->
    <link rel="icon" href="../public/img/Recurso-7.ico">
    <%- include("partials/link") -%>
</head>

<body>

    <%- include("partials/navigation") %>
    
    <%- include("partials/categorias") %>


    <div class="container containerProd p-4">
        <div class="row">
            <h5>Todo lo que necesitas para la decoración de una fiesta inolvidable:</h5>
            <div class="card-columns contenedor-img">
                <% images.reverse().forEach(function (image) { %>
                    <div class="card animated fadeInDown container-producto">
                        <div class="producto-body">
                            <a href="/image/<%= image._id %>">
                                <img src="<%= image.url %>" class="card-img-top" alt="<%= image.filename %>"
                                ondragstart="return false" onselectstart="return false" oncontextmenu="return false">
                            </a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>

</body>

</html>